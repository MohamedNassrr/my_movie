import 'package:flutter/material.dart';

class WarView extends StatelessWidget {
  const WarView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('War screen')
        ],
      ),
    );
  }
}
