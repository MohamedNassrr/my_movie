import 'package:flutter/material.dart';

class ComedyView extends StatelessWidget {
  const ComedyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          Text('Comedy screen')
        ],
      ),
    );
  }
}
