import 'package:flutter/material.dart';

class HorrorView extends StatelessWidget {
  const HorrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Horror screen')
        ],
      ),
    );
  }
}
